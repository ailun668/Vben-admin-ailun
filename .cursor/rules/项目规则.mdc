# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

这是一个基于 Vue 3 + TypeScript 的 Monorepo 支付管理平台，专注于商户端应用开发。

## 技术栈

- **框架**: Vue 3.5.13 + TypeScript 5.7.3
- **构建工具**: Vite 6.0.11 + Turbo 2.4.0
- **包管理器**: pnpm 10.14.0 (强制要求，必须使用 pnpm)
- **UI 组件库**: Ant Design Vue 4.2.6
- **样式方案**: Tailwind CSS 3.4.17
- **状态管理**: Pinia 2.3.1
- **路由**: Vue Router 4.5.0
- **表单验证**: VeeValidate 4.15.0 + Zod 3.24.1
- **HTTP 客户端**: Axios 1.7.9
- **表格组件**: VXE Table 4.10.0
- **图表库**: ECharts 5.6.0

## 常用命令

### 开发环境

```bash
# 启动商户端应用 (端口: 5588)
pnpm dev:mpurse-merchant

# 或使用通用命令
pnpm dev
```

### 构建命令

```bash
# 开发环境构建
pnpm build

# QA 环境构建（包含部署）
pnpm build:mpurse-merchant-qa

# UAT 环境构建（包含部署）
pnpm build:mpurse-merchant-uat

# 生产环境构建（包含部署）
pnpm build:mpurse-merchant-prod

# 仅构建不部署
pnpm build:qa --filter=mpurse-merchant
pnpm build:uat --filter=mpurse-merchant
pnpm build:prod --filter=mpurse-merchant
```

### 代码质量

```bash
# ESLint 检查
pnpm lint

# 代码格式化
pnpm format

# 类型检查
pnpm typecheck
```

### 测试

```bash
# 单元测试
pnpm test:unit

# E2E 测试
pnpm test:e2e
```

### 依赖管理

```bash
# 安装依赖
pnpm install

# 清理并重新安装
pnpm reinstall

# 更新依赖
pnpm update:deps
```

## 项目架构

### Monorepo 结构

```
├── apps/                      # 应用层
│   └── mpurse-merchant/      # 商户端应用 (端口: 5588)
├── packages/                  # 核心包
│   ├── @core/                # 核心功能模块
│   │   ├── base/            # 基础设施 (design, icons, shared, typings)
│   │   ├── composables/     # 组合式函数
│   │   ├── preferences/     # 偏好设置
│   │   └── ui-kit/          # UI 组件集 (form, layout, menu, popup, tabs, shadcn)
│   ├── effects/             # 效果层
│   │   ├── access/          # 权限控制
│   │   ├── common-ui/       # 通用 UI
│   │   ├── hooks/           # 通用 Hooks
│   │   ├── layouts/         # 布局组件
│   │   ├── plugins/         # 插件系统
│   │   └── request/         # HTTP 请求封装
│   ├── constants/           # 常量定义
│   ├── icons/               # 图标资源
│   ├── locales/             # 国际化
│   ├── preferences/         # 用户偏好
│   ├── stores/              # 状态管理
│   ├── styles/              # 全局样式
│   ├── types/               # 类型定义
│   └── utils/               # 工具函数
├── shared/                   # 共享模块
│   ├── adapter/             # 适配器层
│   ├── components/          # 共享组件
│   ├── constants/           # 共享常量
│   ├── hooks/               # 共享 Hooks
│   ├── pages/               # 共享页面
│   ├── request/             # 请求层 (含加密解密)
│   ├── store/               # 共享状态
│   ├── types/               # 共享类型
│   └── utils/               # 共享工具
├── internal/                 # 内部配置
│   ├── eslint-config/       # ESLint 配置
│   ├── tailwind-config/     # Tailwind 配置
│   ├── vite-config/         # Vite 配置
│   └── node-utils/          # Node 工具
└── scripts/                  # 脚本工具
    ├── turbo-run/           # Turbo 运行器
    └── vsh/                 # Shell 脚本
```

### 应用层结构

商户端应用 (mpurse-merchant) 的典型结构：

```
src/
├── adapter/          # 应用适配器
├── components/       # 应用级组件
├── constants/        # 应用常量
├── layouts/          # 布局组件
├── locales/          # 国际化配置
├── models/           # 数据模型
├── request/          # API 请求
├── router/           # 路由配置
├── store/            # 状态管理
├── styles/           # 样式文件
├── utils/            # 工具函数
├── views/            # 页面组件
├── App.vue          # 根组件
├── bootstrap.ts     # 应用引导
├── main.ts          # 入口文件
└── preferences.ts   # 偏好配置
```

## 关键架构设计

### 1. Workspace 依赖管理

项目使用 pnpm workspace + catalog 统一管理依赖版本，所有版本定义在 `pnpm-workspace.yaml` 的 catalog 部分。

### 2. Turbo 构建优化

使用 Turbo 进行任务并行化和缓存管理，配置在 `turbo.json`。所有构建任务使用 `dependsOn: ["^build"]` 确保依赖顺序。

### 3. 路径别名

- `@/` - 指向当前应用的 `src/` 目录
- `@vben/*` - 指向 `packages/` 下的核心模块
- `@shared/*` - 指向 `shared/` 下的共享模块

### 4. 环境配置

应用支持多环境构建：

- `development` - 开发环境
- `qa` - QA 测试环境
- `uat` - UAT 预发布环境
- `prod` - 生产环境

每个应用有独立的 vite.config.mts 配置，通过 `loadEnv` 加载对应环境变量。

### 5. 自动导入

使用 `unplugin-auto-import` 和 `unplugin-vue-components` 实现：

- Vue API 自动导入
- VueUse 自动导入
- Ant Design Vue 组件按需导入
- Store 模块自动导入 (从 `src/store/modules`)

### 6. 加密请求

商户端应用支持可配置的请求加密功能：

**环境变量控制**：

```bash
# .env.development / .env.qa / .env.uat / .env.prod
VITE_NEED_ENCRYPT=true   # 启用加密
VITE_NEED_ENCRYPT=false  # 禁用加密（通常开发环境禁用）
```

**实现位置**：

- `shared/request/` - 加密解密核心逻辑
- `@shared/request` - 商户端使用的请求封装

**使用场景**：

- QA 环境：通常禁用加密便于调试
- UAT/生产环境：启用加密保护敏感数据传输

### 7. API 配置

**API 基础地址**（通过环境变量配置）：

```bash
# .env.development
VITE_GLOB_API_URL='开发环境API地址'

# .env.qa
VITE_GLOB_API_URL='https://qaapi.mpurse.co.in/'

# .env.uat / .env.prod
VITE_GLOB_API_URL='对应环境的API地址'
```

## 开发规范

### Vue 组件

- 使用 `<script setup lang="ts">` 语法
- 组件文件名使用 PascalCase
- 添加完整的 JSDoc 注释
- Props 使用 TypeScript interface 定义
- 优先使用 Composition API

### TypeScript

- 所有新增代码必须有完整类型定义
- 避免使用 `any`，必要时使用 `unknown`
- 接口和类型定义统一放在 `types/` 或 `@vben/types`
- 为公共函数添加 JSDoc 注释

### 样式

- 优先使用 Tailwind CSS 工具类
- 复杂样式使用 `@apply` 指令
- Scoped styles 避免全局污染
- 使用 CSS 变量实现主题切换

### 状态管理

- 使用 Pinia Composition API 风格
- Store 按功能模块划分
- 使用 `pinia-plugin-persistedstate` 持久化关键状态
- 状态变更通过 action 进行

## 重要注意事项

### 强制要求

1. **必须使用 pnpm** - 项目已配置 `preinstall` 脚本强制使用 pnpm
2. **Node.js >= 20.10.0** - 查看 package.json 的 engines 字段
3. **类型安全** - 所有 PR 必须通过 `pnpm typecheck`
4. **代码规范** - 提交前运行 `pnpm lint` 和 `pnpm format`

### 开发工具建议

**参考资源**：

- Vue Vben Admin 官方文档: https://github.com/vbenjs/vue-vben-admin
- 项目基于 Vue Vben Admin 架构，可参考官方文档了解组件和最佳实践
- 在实现新功能前，建议先查看 `shared/` 和 `packages/` 中是否有可复用的模块

### 禁止操作

- 不要修改已验证的核心功能逻辑
- 不要直接修改 node_modules
- 不要删除有用的注释和文档
- 不要忽略 TypeScript 类型错误
- 不要跳过代码检查

### 最佳实践

- 优先使用 `@shared/*` 共享模块而非重复代码
- 新增公共功能应放在 `packages/` 或 `shared/`
- API 请求统一使用 `@vben/request` 或 `@shared/request`
- 表单验证使用 VeeValidate + Zod schema
- 复杂表格使用 VXE Table
- 图表使用 ECharts

## 环境变量

关键环境变量 (参考 `.env.*` 文件):

- `VITE_GLOB_API_URL` - API 基础地址
- `VITE_APP_TITLE` - 应用标题（默认: MPurse Merchant）
- `VITE_NEED_ENCRYPT` - 是否启用请求加密（true/false）
- `VITE_DEV` - 开发模式标识
- `VITE_BASE_PATH` - 应用基础路径（默认: /）
- `NODE_ENV` - 环境标识（development/testing/production）

### 2. 热重载开发

- **组件热更新**: Vite HMR 自动生效，修改 `.vue` 文件即时更新
- **样式热更新**: Tailwind CSS 和 scoped styles 自动热更新
- **Store 热更新**: Pinia 支持 HMR，状态不丢失
- **路由热更新**: 修改路由配置需要手动刷新页面

### 3. 代码审查检查清单

开发完成后，提交前必须检查：

- [ ] `pnpm typecheck` - 无类型错误
- [ ] `pnpm lint` - 无 ESLint 错误
- [ ] `pnpm format` - 代码已格式化
- [ ] 浏览器控制台无错误和警告
- [ ] 新增功能有完整的类型定义
- [ ] 公共函数有 JSDoc 注释
- [ ] 可复用代码已提取到 `shared/` 或 `packages/`

### 常见错误码处理

```typescript
// 在 shared/request/ 中统一处理
401 - 未授权，跳转登录页
403 - 无权限，提示用户
404 - 资源不存在
500 - 服务器错误，记录日志
502/503 - 服务不可用，显示维护页面
```

### 5. 发布检查清单

发布前必须确认：

- [ ] 所有测试用例通过
- [ ] 类型检查无错误 (`pnpm typecheck`)
- [ ] 代码规范检查通过 (`pnpm lint`)
- [ ] 构建成功无错误
- [ ] 环境变量配置正确
- [ ] 数据库迁移脚本已执行（如有）
- [ ] API 兼容性已确认
- [ ] 相关文档已更新
- [ ] 回滚方案已准备
- [ ] 监控和日志已配置

## 性能优化建议

### 1. 构建优化

```bash
# 分析构建产物大小
pnpm build:prod --filter=mpurse-merchant
# 查看 dist/report.html 分析报告

# 常见优化点：
# - 图片压缩和 lazy loading
# - 路由懒加载
# - 组件库按需导入（已配置 Ant Design Vue）
# - 代码分割和 chunk 优化
# - 合理使用请求加密（生产环境启用，开发环境禁用）
```

### 2. 运行时优化

```vue
<!-- 使用 v-memo 缓存渲染结果 -->
<template>
  <div v-memo="[item.id]">
    {{ expensiveComputation(item) }}
  </div>
</template>

<!-- 使用 v-once 渲染一次性内容 -->
<div v-once>{{ staticContent }}</div>

<!-- 列表使用正确的 key -->
<div v-for="item in list" :key="item.id">
```

### 3. 状态管理优化

```typescript
// 避免在 store 中存储过大的数据
// 使用 computed 派生状态
// 合理使用持久化插件（仅持久化必要数据）
```

## 安全注意事项

### 2. XSS 防护

```vue
<!-- ❌ 避免使用 v-html（除非必要） -->
<div v-html="userInput"></div>

<!-- ✅ 使用文本插值（自动转义） -->
<div>{{ userInput }}</div>

<!-- ✅ 必须使用 v-html 时，先清理内容 -->
<div v-html="sanitize(userInput)"></div>
```

### 3. API 安全

```typescript
// 商户端使用加密请求（已在 shared/request/ 实现）
// 敏感数据传输加密
// Token 存储在 httpOnly cookie（如可能）
```

### MCP工具决策流程图

```
任务复杂度评估
├── 单文件修改 → 直接编辑
├── 2-3个相关文件 → 考虑使用context7
├── 跨模块影响 → 必须使用context7
└── API设计变更 → 必须使用context7
```

**🚨 MCP工具选择重要提醒**

- **context7**: 用于理解项目代码结构和逻辑关系
- Ant Design Vue: 用于获取UI组件的官方文档和最佳实践
- **组合使用**: 复杂的UI功能开发时，先用Ant Design Vue    查找组件方案，再用context7分析项目结构
- **确保一致性**: 使用Vue Vben Admin  MCP获取的方案要与项目现有的组件模式保持一致

## 参考文档

- Vue Vben Admin: https://github.com/vbenjs/vue-vben-admin
- Ant Design Vue: https://antdv.com/docs/vue/introduce
- Vue 3: https://v3.cn.vuejs.org/
- Pinia: https://pinia.vuejs.org/
- Tailwind CSS: https://tailwindcss.com/
- Changesets: https://github.com/changesets/changesets
