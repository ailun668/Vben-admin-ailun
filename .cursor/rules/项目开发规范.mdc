---
description: Vue 3 + TypeScript + Ant Design Vue 项目开发规范，整合 Ant Design Vue、Vue Vben Admin 最佳实践
globs: **/*.{vue,ts,js,tsx,jsx}
alwaysApply: true
---
# Vue 3 项目开发规范

> 基于 Ant Design Vue、Vue Vben Admin 最佳实践的项目开发规范

## 📋 项目概述

本项目是基于 **Vue 3 + TypeScript + Vite + Ant Design Vue** 的企业级中后台管理系统。

### 技术栈

- **框架**: Vue 3.5+ (Composition API with `<script setup>`)
- **语言**: TypeScript 5.9+ (严格模式)
- **构建工具**: Vite 7.2+
- **状态管理**: Pinia 3.0+ (with persistence plugin)
- **UI 组件库**: Ant Design Vue 4.2+
- **路由**: Vue Router 4.6+
- **HTTP 客户端**: Axios
- **图标**: @ant-design/icons-vue
- **工具库**: lodash-es, dayjs, crypto-js, nprogress, qs

---

## 🏗️ 项目结构规范

### 目录结构

```
src/
├── api/              # API 服务层
│   ├── http.ts       # Axios 实例配置
│   ├── types.ts      # API 类型定义
│   └── *.ts          # 各模块 API
├── assets/           # 静态资源
├── components/       # 可复用组件
│   └── Crud/         # CRUD 组件
├── mock/             # Mock API 数据
├── router/           # 路由配置
│   └── index.ts      # 路由定义和守卫
├── store/            # Pinia 状态管理
│   ├── modules/      # Store 模块
│   │   ├── user.ts   # 用户状态
│   │   ├── permission.ts  # 权限状态
│   │   └── app.ts    # 应用状态
│   └── index.ts      # Store 入口
├── views/            # 页面组件
├── utils/            # 工具函数
├── App.vue           # 根组件
└── main.ts           # 应用入口
```

### 文件命名规范

- **组件文件**: PascalCase (如 `UserCard.vue`, `AddUserModal.vue`)
- **工具文件**: camelCase (如 `formatDate.ts`, `validateEmail.ts`)
- **类型文件**: camelCase (如 `types.ts`, `apiTypes.ts`)
- **页面文件**: PascalCase (如 `UsersView.vue`, `DashboardView.vue`)

---

## 💻 Vue 组件开发规范

### 组件结构

```vue
<template>
  <!-- 1. 模板部分 -->
  <div class="component-container">
    <!-- 使用语义化 HTML -->
  </div>
</template>

<script setup lang="ts">
/**
 * @fileoverview 组件功能描述
 * @author 开发者名称
 * @date 创建日期
 */

// 2. 导入依赖
import { ref, computed, onMounted } from 'vue'
import { Button, Input } from 'ant-design-vue'
import type { ComponentProps } from './types'

// 3. 类型定义
interface Props {
  /** 标题 */
  title: string
  /** 是否可见 */
  visible?: boolean
}

interface Emits {
  /** 确认事件 */
  (e: 'confirm', value: string): void
  /** 取消事件 */
  (e: 'cancel'): void
}

// 4. Props 和 Emits
const props = withDefaults(defineProps<Props>(), {
  visible: true
})

const emit = defineEmits<Emits>()

// 5. 响应式数据
const loading = ref(false)
const formData = ref({})

// 6. 计算属性
const isDisabled = computed(() => loading.value || !formData.value.name)

// 7. 方法
const handleSubmit = async () => {
  loading.value = true
  try {
    // 业务逻辑
    emit('confirm', formData.value)
  } catch (error) {
    console.error('提交失败:', error)
  } finally {
    loading.value = false
  }
}

// 8. 生命周期
onMounted(() => {
  // 初始化逻辑
})
</script>

<style scoped>
/* 9. 样式部分 */
.component-container {
  padding: 16px;
}
</style>
```

### 组件开发要求

1. **必须使用 `<script setup>` 语法**
2. **所有组件必须有 JSDoc 注释**
3. **Props 必须定义类型和默认值**
4. **使用 TypeScript 严格类型检查**
5. **组件名使用 PascalCase**
6. **单文件组件顺序：template → script → style**

---

## 📝 TypeScript 规范

### 类型定义

```typescript
/**
 * 用户信息接口
 */
export interface UserInfo {
  /** 用户ID */
  id: number
  /** 用户名 */
  username: string
  /** 真实姓名 */
  realName: string
  /** 邮箱 */
  email: string
  /** 头像 */
  avatar?: string
  /** 角色列表 */
  roles: string[]
  /** 权限列表 */
  permissions: string[]
}

/**
 * API 响应结构
 */
export interface ApiResponse<T = any> {
  /** 响应码 */
  code: number
  /** 响应消息 */
  message: string
  /** 响应数据 */
  data: T
  /** 是否成功 */
  success?: boolean
}

/**
 * 分页参数
 */
export interface PageParams {
  /** 当前页码 */
  page: number
  /** 每页大小 */
  size: number
  /** 排序字段 */
  sort?: string
  /** 排序方向 */
  order?: 'asc' | 'desc'
}
```

### TypeScript 要求

1. **启用严格模式** (`strict: true`)
2. **禁止使用 `any`，使用 `unknown` 替代**
3. **所有函数必须有明确的返回类型**
4. **使用接口定义对象类型**
5. **使用类型别名定义联合类型**
6. **充分利用泛型提高代码复用性**

---

## 🎨 Ant Design Vue 使用规范

### 组件引入

```typescript
// ✅ 推荐：按需引入
import { Button, Input, Table, Form, Modal } from 'ant-design-vue'
import { UserOutlined, SettingOutlined } from '@ant-design/icons-vue'

// ❌ 不推荐：全量引入
import Antd from 'ant-design-vue'
```

### 表单使用

```vue
<template>
  <a-form
    :model="formState"
    :rules="rules"
    :label-col="{ span: 6 }"
    :wrapper-col="{ span: 18 }"
    @finish="onFinish"
    @finishFailed="onFinishFailed"
  >
    <a-form-item label="用户名" name="username">
      <a-input v-model:value="formState.username" placeholder="请输入用户名" />
    </a-form-item>
    
    <a-form-item label="密码" name="password">
      <a-input-password v-model:value="formState.password" placeholder="请输入密码" />
    </a-form-item>
    
    <a-form-item :wrapper-col="{ offset: 6, span: 18 }">
      <a-button type="primary" html-type="submit" :loading="loading">
        提交
      </a-button>
      <a-button @click="handleReset">重置</a-button>
    </a-form-item>
  </a-form>
</template>

<script setup lang="ts">
import { reactive, ref } from 'vue'
import { Form, Input, Button, message } from 'ant-design-vue'
import type { Rule } from 'ant-design-vue/es/form'

interface FormState {
  username: string
  password: string
}

const formState = reactive<FormState>({
  username: '',
  password: ''
})

const loading = ref(false)

const rules: Record<string, Rule[]> = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 3, max: 20, message: '长度在 3 到 20 个字符', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, message: '密码至少 6 个字符', trigger: 'blur' }
  ]
}

const onFinish = async (values: FormState) => {
  loading.value = true
  try {
    // API 调用
    message.success('提交成功')
  } catch (error) {
    message.error('提交失败')
  } finally {
    loading.value = false
  }
}

const onFinishFailed = (errors: any) => {
  console.error('表单验证失败:', errors)
}

const handleReset = () => {
  formState.username = ''
  formState.password = ''
}
</script>
```

### 表格使用

```vue
<template>
  <a-table
    :columns="columns"
    :data-source="dataSource"
    :loading="loading"
    :pagination="pagination"
    @change="handleTableChange"
  >
    <template #bodyCell="{ column, record }">
      <template v-if="column.key === 'action'">
        <a-button type="link" @click="handleEdit(record)">编辑</a-button>
        <a-button type="link" danger @click="handleDelete(record)">删除</a-button>
      </template>
    </template>
  </a-table>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { Table, Button } from 'ant-design-vue'
import type { ColumnsType, TableProps } from 'ant-design-vue/es/table'

interface DataType {
  key: string
  name: string
  age: number
  address: string
}

const columns: ColumnsType<DataType> = [
  {
    title: '姓名',
    dataIndex: 'name',
    key: 'name'
  },
  {
    title: '年龄',
    dataIndex: 'age',
    key: 'age',
    sorter: (a, b) => a.age - b.age
  },
  {
    title: '地址',
    dataIndex: 'address',
    key: 'address'
  },
  {
    title: '操作',
    key: 'action',
    fixed: 'right',
    width: 150
  }
]

const dataSource = ref<DataType[]>([])
const loading = ref(false)
const pagination = ref({
  current: 1,
  pageSize: 10,
  total: 0,
  showSizeChanger: true,
  showTotal: (total: number) => `共 ${total} 条`
})

const handleTableChange: TableProps['onChange'] = (pag, filters, sorter) => {
  pagination.value.current = pag.current || 1
  pagination.value.pageSize = pag.pageSize || 10
  // 重新加载数据
  loadData()
}

const loadData = async () => {
  loading.value = true
  try {
    // API 调用
  } catch (error) {
    console.error('加载数据失败:', error)
  } finally {
    loading.value = false
  }
}

const handleEdit = (record: DataType) => {
  console.log('编辑:', record)
}

const handleDelete = (record: DataType) => {
  console.log('删除:', record)
}
</script>
```

### Ant Design Vue 使用要求

1. **使用 `v-model:value` 而不是 `v-model`**
2. **表单验证使用 `rules` 属性**
3. **表格使用 `columns` 和 `data-source` 属性**
4. **图标从 `@ant-design/icons-vue` 引入**
5. **消息提示使用 `message` 或 `notification`**

---

## 🔄 状态管理规范 (Pinia)

### Store 定义

```typescript
/**
 * @fileoverview 用户状态管理
 */
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { UserInfo } from '@/api/types'
import { getUserInfo, login, logout } from '@/api/user'

export const useUserStore = defineStore('user', () => {
  // 状态
  const token = ref<string>('')
  const userInfo = ref<UserInfo | null>(null)
  const loading = ref(false)

  // 计算属性
  const isLoggedIn = computed(() => !!token.value)
  const userName = computed(() => userInfo.value?.realName || userInfo.value?.username || '用户')
  const userRoles = computed(() => userInfo.value?.roles || [])

  // 方法
  /**
   * 登录
   * @param credentials 登录凭据
   */
  const loginAction = async (credentials: LoginCredentials) => {
    loading.value = true
    try {
      const { data } = await login(credentials)
      token.value = data.token
      userInfo.value = data.user
      return { success: true }
    } catch (error) {
      console.error('登录失败:', error)
      return { success: false, error }
    } finally {
      loading.value = false
    }
  }

  /**
   * 获取当前用户信息
   */
  const getCurrentUser = async () => {
    if (!token.value) return
    loading.value = true
    try {
      const { data } = await getUserInfo()
      userInfo.value = data
    } catch (error) {
      console.error('获取用户信息失败:', error)
      token.value = ''
      userInfo.value = null
    } finally {
      loading.value = false
    }
  }

  /**
   * 登出
   */
  const logoutAction = async () => {
    try {
      await logout()
    } catch (error) {
      console.error('登出失败:', error)
    } finally {
      token.value = ''
      userInfo.value = null
    }
  }

  /**
   * 检查用户角色
   * @param role 角色名称
   */
  const hasRole = (role: string): boolean => {
    return userRoles.value.includes(role)
  }

  return {
    // 状态
    token,
    userInfo,
    loading,
    // 计算属性
    isLoggedIn,
    userName,
    userRoles,
    // 方法
    login: loginAction,
    getCurrentUser,
    logout: logoutAction,
    hasRole
  }
}, {
  // 持久化配置
  persist: {
    key: 'user-store',
    paths: ['token', 'userInfo']
  }
})
```

### Pinia 使用要求

1. **使用 Composition API 风格定义 Store**
2. **状态使用 `ref`，计算属性使用 `computed`**
3. **方法使用普通函数，不是箭头函数**
4. **重要状态需要持久化到 localStorage**
5. **Store 命名使用 camelCase，文件名使用 kebab-case**

---

## 🌐 API 请求规范

### HTTP 客户端配置

```typescript
/**
 * @fileoverview HTTP 客户端配置
 */
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import { message } from 'ant-design-vue'
import { useUserStore } from '@/store/modules/user'
import type { ApiResponse } from './types'

const http: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
http.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    const userStore = useUserStore()
    if (userStore.token && config.headers) {
      config.headers.Authorization = `Bearer ${userStore.token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
http.interceptors.response.use(
  (response: AxiosResponse<ApiResponse>) => {
    const { code, data, message: msg } = response.data
    
    // 业务成功
    if (code === 0) {
      return data
    }
    
    // 业务失败
    message.error(msg || '请求失败')
    return Promise.reject(new Error(msg || '请求失败'))
  },
  (error) => {
    // HTTP 错误处理
    if (error.response) {
      const { status } = error.response
      
      switch (status) {
        case 401:
          message.error('未授权，请重新登录')
          const userStore = useUserStore()
          userStore.logout()
          // 跳转到登录页
          break
        case 403:
          message.error('拒绝访问')
          break
        case 404:
          message.error('请求地址不存在')
          break
        case 500:
          message.error('服务器内部错误')
          break
        default:
          message.error('网络错误')
      }
    } else {
      message.error('网络连接失败')
    }
    
    return Promise.reject(error)
  }
)

export default http
```

### API 函数定义

```typescript
/**
 * @fileoverview 用户相关 API
 */
import http from './http'
import type { ApiResponse, UserInfo, LoginRequest, LoginResponse } from './types'

/**
 * 用户登录
 * @param data 登录数据
 * @returns 登录响应
 */
export const login = (data: LoginRequest) => {
  return http.post<LoginResponse>('/user/login', data)
}

/**
 * 获取当前用户信息
 * @returns 用户信息
 */
export const getUserInfo = () => {
  return http.get<UserInfo>('/user/info')
}

/**
 * 用户登出
 */
export const logout = () => {
  return http.post('/user/logout')
}

/**
 * 获取用户列表
 * @param params 查询参数
 * @returns 用户列表
 */
export const getUserList = (params: PageParams) => {
  return http.get<ApiResponse<UserInfo[]>>('/user/list', { params })
}
```

### API 使用要求

1. **统一使用封装的 HTTP 客户端**
2. **所有 API 函数必须有 JSDoc 注释**
3. **使用 TypeScript 类型定义请求和响应**
4. **错误处理在拦截器中统一处理**
5. **请求参数使用对象，不使用多个参数**

---

## 🛣️ 路由规范

### 路由配置

```typescript
/**
 * @fileoverview 路由配置
 */
import { createRouter, createWebHistory, RouteRecordRaw } from 'vue-router'
import { useUserStore } from '@/store/modules/user'
import { usePermissionStore } from '@/store/modules/permission'

const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/LoginView.vue'),
    meta: {
      title: '登录',
      requiresAuth: false
    }
  },
  {
    path: '/',
    name: 'Home',
    component: () => import('@/views/HomeView.vue'),
    meta: {
      title: '首页',
      requiresAuth: true,
      roles: ['admin', 'user']
    }
  },
  {
    path: '/users',
    name: 'Users',
    component: () => import('@/views/UsersView/UsersView.vue'),
    meta: {
      title: '用户管理',
      requiresAuth: true,
      roles: ['admin'],
      icon: 'user'
    }
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach(async (to, from, next) => {
  const userStore = useUserStore()
  const permissionStore = usePermissionStore()

  // 检查是否需要认证
  if (to.meta.requiresAuth) {
    if (!userStore.token) {
      next({ name: 'Login', query: { redirect: to.fullPath } })
      return
    }

    // 获取用户信息
    if (!userStore.userInfo) {
      await userStore.getCurrentUser()
    }

    // 检查角色权限
    const roles = to.meta.roles as string[]
    if (roles && roles.length > 0) {
      const hasPermission = roles.some(role => userStore.hasRole(role))
      if (!hasPermission) {
        next({ name: '403' })
        return
      }
    }
  }

  next()
})

export default router
```

### 路由 Meta 配置

```typescript
interface RouteMeta {
  /** 页面标题 */
  title: string
  /** 是否需要认证 */
  requiresAuth?: boolean
  /** 允许访问的角色 */
  roles?: string[]
  /** 菜单图标 */
  icon?: string
  /** 是否在菜单中隐藏 */
  hideInMenu?: boolean
  /** 是否缓存页面 */
  keepAlive?: boolean
}
```

### 路由使用要求

1. **使用路由懒加载** (`() => import()`)
2. **路由 Meta 必须包含 `title`**
3. **权限控制使用 `roles` 数组**
4. **路由守卫统一处理认证和权限**

---

## 🎨 样式规范

### CSS 编写规范

```vue
<style scoped>
/* 1. 使用 scoped 避免样式污染 */
.component-container {
  padding: 16px;
  background: #fff;
}

/* 2. 使用 BEM 命名规范（可选） */
.component-container__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.component-container__body {
  margin-top: 16px;
}

/* 3. 使用 CSS 变量 */
.component-container {
  --primary-color: #1890ff;
  color: var(--primary-color);
}

/* 4. 深度选择器（谨慎使用） */
:deep(.ant-btn) {
  margin-right: 8px;
}
</style>
```

### 样式要求

1. **所有样式使用 `scoped`**
2. **优先使用 Ant Design Vue 的样式类**
3. **使用 CSS 变量实现主题定制**
4. **深度选择器使用 `:deep()`**
5. **避免使用 `!important`**

---

## 📚 代码注释规范

### JSDoc 注释

```typescript
/**
 * @fileoverview 用户工具函数
 * @author 开发者名称
 * @date 2024-01-01
 */

/**
 * 格式化用户名
 * @param firstName 名
 * @param lastName 姓
 * @returns 格式化后的用户名
 * @example
 * formatUserName('John', 'Doe') // 'John Doe'
 */
export const formatUserName = (firstName: string, lastName: string): string => {
  return `${firstName} ${lastName}`
}

/**
 * 验证邮箱格式
 * @param email 邮箱地址
 * @returns 是否有效
 */
export const validateEmail = (email: string): boolean => {
  const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  return regex.test(email)
}
```

### 注释要求

1. **所有函数必须有 JSDoc 注释**
2. **复杂逻辑必须有行内注释**
3. **组件文件必须有 `@fileoverview` 注释**
4. **参数和返回值必须注释**
5. **提供使用示例（如适用）**

---

## ✅ 代码质量要求

### 必须遵循

1. **TypeScript 严格模式，无类型错误**
2. **ESLint 检查通过，无警告**
3. **所有函数必须有 JSDoc 注释**
4. **组件必须使用 `<script setup>` 语法**
5. **Props 必须定义类型和默认值**
6. **API 调用必须有错误处理**
7. **异步操作必须使用 `async/await`**
8. **避免使用 `any` 类型**

### 性能优化

1. **路由使用懒加载**
2. **组件按需引入**
3. **图片使用 WebP 格式**
4. **合理使用 `computed` 和 `watch`**
5. **避免不必要的响应式数据**

### 安全要求

1. **用户输入必须验证和转义**
2. **敏感信息不得暴露在前端**
3. **API 请求使用 HTTPS**
4. **权限检查前后端双重验证**

---

## 🚫 禁止事项

1. ❌ **禁止使用 `any` 类型**
2. ❌ **禁止直接修改 Props**
3. ❌ **禁止在模板中使用复杂逻辑**
4. ❌ **禁止忽略 TypeScript 类型错误**
5. ❌ **禁止使用 `var` 声明变量**
6. ❌ **禁止使用 `==`，必须使用 `===`**
7. ❌ **禁止在循环中创建函数**
8. ❌ **禁止忽略错误处理**

---

## 📖 参考资源

- [Vue 3 官方文档](https://vuejs.org/)
- [TypeScript 官方文档](https://www.typescriptlang.org/)
- [Ant Design Vue 官方文档](https://antdv.com/)
- [Vue Vben Admin 文档](https://doc.vben.pro/)
- [Pinia 官方文档](https://pinia.vuejs.org/)
- [Vue Router 官方文档](https://router.vuejs.org/)

---

**最后更新**: 2025-01-XX  
**维护者**: 开发团队
