# 核心组件快速参考

## 🚀 快速开始

### 1. 项目启动
```bash
# 安装依赖
pnpm install

# 启动开发环境
pnpm dev:mpurse-merchant

# 访问地址: http://localhost:5588
```

### 2. 常用命令
```bash
# 代码质量检查
pnpm lint          # ESLint 检查
pnpm format        # 代码格式化
pnpm typecheck     # 类型检查

# 构建部署
pnpm build:qa      # QA 环境构建
pnpm build:uat     # UAT 环境构建
pnpm build:prod    # 生产环境构建
```

## 📦 核心组件速查

### Vue Vben Admin 组件

| 组件 | 用途 | 基础用法 |
|------|------|----------|
| **VbenModal** | 模态框 | `const [Modal, modalApi] = useVbenModal({ title: '标题' })` |
| **VbenDrawer** | 抽屉 | `const [Drawer, drawerApi] = useVbenDrawer({ placement: 'right' })` |
| **VbenVxeGrid** | 表格 | `const [Grid, gridApi] = useVbenVxeGrid({ gridOptions: {} })` |
| **ApiComponent** | API组件 | `<ApiComponent :api="fetchData" :component="Select" />` |
| **AccessControl** | 权限控制 | `<AccessControl :codes="['admin']"><Button>管理员可见</Button></AccessControl>` |

### Ant Design Vue 组件

| 组件 | 用途 | 基础用法 |
|------|------|----------|
| **Button** | 按钮 | `<a-button type="primary">按钮</a-button>` |
| **Input** | 输入框 | `<a-input v-model:value="value" placeholder="请输入" />` |
| **Select** | 选择器 | `<a-select v-model:value="selected"><a-select-option value="1">选项1</a-select-option></a-select>` |
| **Table** | 表格 | `<a-table :columns="columns" :data-source="data" />` |
| **Form** | 表单 | `<a-form :model="form" :rules="rules"><a-form-item label="字段"><a-input /></a-form-item></a-form>` |
| **Card** | 卡片 | `<a-card title="标题"><p>内容</p></a-card>` |
| **Modal** | 模态框 | `modal.confirm({ title: '确认', content: '内容' })` |
| **Message** | 消息 | `message.success('成功')` |
| **Notification** | 通知 | `notification.open({ message: '通知' })` |

## 🎯 常用模式

### 1. 表格 + 搜索表单
```vue
<script setup>
const [Grid, gridApi] = useVbenVxeGrid({
  gridOptions: {
    columns: [
      { field: 'name', title: '姓名' },
      { field: 'status', title: '状态' }
    ]
  },
  formOptions: {
    schemas: [
      { field: 'name', label: '姓名', component: 'Input' },
      { field: 'status', label: '状态', component: 'Select' }
    ]
  }
});
</script>

<template>
  <Grid />
</template>
```

### 2. 模态框 + 表单
```vue
<script setup>
const [Modal, modalApi] = useVbenModal({
  title: '编辑用户',
  connectedComponent: UserFormComponent,
  onConfirm: () => {
    const data = modalApi.getData();
    console.log('表单数据:', data);
  }
});
</script>

<template>
  <Modal />
</template>
```

### 3. API 组件 + 选择器
```vue
<template>
  <ApiComponent
    :api="fetchUserList"
    :component="Select"
    label-field="name"
    value-field="id"
    immediate
  />
</template>

<script setup>
const fetchUserList = async () => {
  const response = await fetch('/api/users');
  return response.json();
};
</script>
```

## 🔧 开发工具

### 路径别名
- `@/` - 当前应用的 `src/` 目录
- `@vben/*` - `packages/` 下的核心模块
- `@shared/*` - `shared/` 下的共享模块

### 环境变量
```bash
# 开发环境
VITE_GLOB_API_URL='开发环境API地址'
VITE_NEED_ENCRYPT=false

# QA 环境
VITE_GLOB_API_URL='https://qaapi.mpurse.co.in/'
VITE_NEED_ENCRYPT=false

# 生产环境
VITE_GLOB_API_URL='生产环境API地址'
VITE_NEED_ENCRYPT=true
```

### TypeScript 类型
```typescript
// 用户信息类型
interface UserInfo {
  id: number;
  name: string;
  email: string;
  status: 0 | 1;
}

// API 响应类型
interface ApiResponse<T> {
  code: number;
  message: string;
  data: T;
}

// 分页参数类型
interface PaginationParams {
  current: number;
  pageSize: number;
  total?: number;
}
```

## 🚨 常见问题

### 1. 组件导入问题
```typescript
// ✅ 正确 - 按需导入
import { Button, Input, Select } from 'ant-design-vue';

// ❌ 错误 - 全局导入（已配置）
// import Antd from 'ant-design-vue';
```

### 2. 样式问题
```vue
<!-- ✅ 正确 - 使用 Tailwind CSS -->
<div class="bg-white rounded-lg shadow-sm p-6">

<!-- ❌ 错误 - 避免内联样式 -->
<div style="background: white; border-radius: 8px; padding: 24px;">
```

### 3. 权限控制
```vue
<!-- ✅ 正确 - 使用 AccessControl -->
<AccessControl :codes="['admin']">
  <Button>管理员功能</Button>
</AccessControl>

<!-- ❌ 错误 - 手动判断权限 -->
<Button v-if="userRole === 'admin'">管理员功能</Button>
```

## 📚 学习资源

- [Vue Vben Admin 官方文档](https://github.com/vbenjs/vue-vben-admin)
- [Ant Design Vue 官方文档](https://antdv.com/docs/vue/introduce)
- [Vue 3 官方文档](https://v3.cn.vuejs.org/)
- [TypeScript 官方文档](https://www.typescriptlang.org/)

## 🆘 获取帮助

1. **查看完整文档**: `docs/核心组件/核心组件开发指南.md`
2. **项目规则**: `.cursor/rules/项目规则.mdc`
3. **智能体配置**: `.cursor/rules/智能体.mdc`
4. **技术栈文档**: `docs/0-Admin 前端技术栈文档.md`

---

**快速参考版本**: v1.0.0  
**最后更新**: 2024年12月
