# 核心组件示例集合

## 📋 文档说明

本文档提供了 MPurse 支付管理平台中常用组件的完整示例代码，方便开发者快速复制和使用。

## 🏗️ Vue Vben Admin 组件示例

### 1. VbenModal 模态框示例

#### 基础模态框
```vue
<template>
  <div>
    <a-button type="primary" @click="openModal">打开模态框</a-button>
    <Modal />
  </div>
</template>

<script setup lang="ts">
import { useVbenModal } from '@vben/common-ui';

const [Modal, modalApi] = useVbenModal({
  title: '用户信息',
  width: 600,
  onConfirm: () => {
    console.log('确认操作');
    modalApi.close();
  },
  onCancel: () => {
    console.log('取消操作');
    modalApi.close();
  }
});

const openModal = () => {
  modalApi.open();
};
</script>
```

#### 带表单的模态框
```vue
<template>
  <div>
    <a-button type="primary" @click="openEditModal">编辑用户</a-button>
    <Modal />
  </div>
</template>

<script setup lang="ts">
import { useVbenModal } from '@vben/common-ui';
import UserEditForm from './UserEditForm.vue';

const [Modal, modalApi] = useVbenModal({
  title: '编辑用户',
  width: 800,
  connectedComponent: UserEditForm,
  onConfirm: () => {
    const formData = modalApi.getData();
    console.log('表单数据:', formData);
    // 调用 API 保存数据
    saveUser(formData);
  }
});

const openEditModal = (userData?: any) => {
  modalApi.setData(userData || {});
  modalApi.open();
};

const saveUser = async (data: any) => {
  try {
    modalApi.setState({ loading: true });
    // await userApi.update(data);
    message.success('保存成功');
    modalApi.close();
  } catch (error) {
    message.error('保存失败');
  } finally {
    modalApi.setState({ loading: false });
  }
};
</script>
```

### 2. VbenDrawer 抽屉示例

#### 基础抽屉
```vue
<template>
  <div>
    <a-button type="primary" @click="openDrawer">打开抽屉</a-button>
    <Drawer />
  </div>
</template>

<script setup lang="ts">
import { useVbenDrawer } from '@vben/common-ui';

const [Drawer, drawerApi] = useVbenDrawer({
  title: '设置面板',
  placement: 'right',
  width: 400,
  onConfirm: () => {
    console.log('确认设置');
    drawerApi.close();
  }
});

const openDrawer = () => {
  drawerApi.open();
};
</script>
```

#### 带表单的抽屉
```vue
<template>
  <div>
    <a-button type="primary" @click="openSettingsDrawer">系统设置</a-button>
    <Drawer>
      <template #default>
        <a-form :model="settings" layout="vertical">
          <a-form-item label="系统名称">
            <a-input v-model:value="settings.systemName" />
          </a-form-item>
          <a-form-item label="系统描述">
            <a-textarea v-model:value="settings.description" :rows="4" />
          </a-form-item>
          <a-form-item label="启用通知">
            <a-switch v-model:checked="settings.notifications" />
          </a-form-item>
        </a-form>
      </template>
    </Drawer>
  </div>
</template>

<script setup lang="ts">
import { reactive } from 'vue';
import { useVbenDrawer } from '@vben/common-ui';

const settings = reactive({
  systemName: 'MPurse 支付系统',
  description: '企业级支付管理平台',
  notifications: true
});

const [Drawer, drawerApi] = useVbenDrawer({
  title: '系统设置',
  placement: 'right',
  width: 500,
  onConfirm: () => {
    console.log('保存设置:', settings);
    // 保存设置逻辑
    drawerApi.close();
  }
});

const openSettingsDrawer = () => {
  drawerApi.open();
};
</script>
```

### 3. VbenVxeGrid 表格示例

#### 基础表格
```vue
<template>
  <div>
    <Grid />
  </div>
</template>

<script setup lang="ts">
import { useVbenVxeGrid } from '#/adapter/vxe-table';

const [Grid, gridApi] = useVbenVxeGrid({
  gridOptions: {
    columns: [
      { field: 'id', title: 'ID', width: 80 },
      { field: 'name', title: '姓名', width: 120 },
      { field: 'email', title: '邮箱', width: 200 },
      { field: 'phone', title: '电话', width: 150 },
      { field: 'status', title: '状态', width: 100 },
      { field: 'createTime', title: '创建时间', width: 180 }
    ],
    data: [
      {
        id: 1,
        name: '张三',
        email: 'zhangsan@example.com',
        phone: '13800138000',
        status: 1,
        createTime: '2024-01-01 10:00:00'
      },
      {
        id: 2,
        name: '李四',
        email: 'lisi@example.com',
        phone: '13800138001',
        status: 0,
        createTime: '2024-01-02 11:00:00'
      }
    ],
    pagerConfig: {
      enabled: true,
      pageSize: 10,
      pageSizes: [10, 20, 50, 100]
    }
  }
});
</script>
```

#### 带搜索表单的表格
```vue
<template>
  <div>
    <Grid />
  </div>
</template>

<script setup lang="ts">
import { useVbenVxeGrid } from '#/adapter/vxe-table';

const [Grid, gridApi] = useVbenVxeGrid({
  gridOptions: {
    columns: [
      { field: 'id', title: 'ID', width: 80 },
      { field: 'name', title: '姓名', width: 120 },
      { field: 'email', title: '邮箱', width: 200 },
      { field: 'status', title: '状态', width: 100 },
      { field: 'createTime', title: '创建时间', width: 180 },
      { field: 'action', title: '操作', width: 150 }
    ],
    pagerConfig: {
      enabled: true,
      pageSize: 10
    }
  },
  formOptions: {
    schemas: [
      {
        field: 'name',
        label: '姓名',
        component: 'Input',
        componentProps: {
          placeholder: '请输入姓名'
        }
      },
      {
        field: 'email',
        label: '邮箱',
        component: 'Input',
        componentProps: {
          placeholder: '请输入邮箱'
        }
      },
      {
        field: 'status',
        label: '状态',
        component: 'Select',
        componentProps: {
          placeholder: '请选择状态',
          options: [
            { label: '启用', value: 1 },
            { label: '禁用', value: 0 }
          ]
        }
      },
      {
        field: 'createTime',
        label: '创建时间',
        component: 'RangePicker',
        componentProps: {
          placeholder: ['开始时间', '结束时间']
        }
      }
    ]
  },
  gridEvents: {
    onCellClick: ({ row, column }) => {
      console.log('点击单元格:', row, column);
    }
  }
});
</script>
```

### 4. ApiComponent 示例

#### 基础 API 组件
```vue
<template>
  <div>
    <ApiComponent
      :api="fetchUserList"
      :component="Select"
      label-field="name"
      value-field="id"
      immediate
    />
  </div>
</template>

<script setup lang="ts">
import { ApiComponent } from '@vben/common-ui';
import { Select } from 'ant-design-vue';

const fetchUserList = async () => {
  try {
    const response = await fetch('/api/users');
    const data = await response.json();
    return data.list || [];
  } catch (error) {
    console.error('获取用户列表失败:', error);
    return [];
  }
};
</script>
```

#### 级联选择器 API 组件
```vue
<template>
  <div>
    <ApiComponent
      :api="fetchRegionData"
      :component="Cascader"
      children-field="children"
      loading-slot="suffixIcon"
      visible-event="onDropdownVisibleChange"
    />
  </div>
</template>

<script setup lang="ts">
import { ApiComponent } from '@vben/common-ui';
import { Cascader } from 'ant-design-vue';

const fetchRegionData = async () => {
  return [
    {
      label: '北京市',
      value: 'beijing',
      children: [
        {
          label: '朝阳区',
          value: 'chaoyang',
          children: [
            { label: '三里屯街道', value: 'sanlitun' },
            { label: '建国门街道', value: 'jianguomen' }
          ]
        },
        {
          label: '海淀区',
          value: 'haidian',
          children: [
            { label: '中关村街道', value: 'zhongguancun' },
            { label: '学院路街道', value: 'xueyuanlu' }
          ]
        }
      ]
    },
    {
      label: '上海市',
      value: 'shanghai',
      children: [
        {
          label: '浦东新区',
          value: 'pudong',
          children: [
            { label: '陆家嘴街道', value: 'lujiazui' },
            { label: '张江镇', value: 'zhangjiang' }
          ]
        }
      ]
    }
  ];
};
</script>
```

## 🎨 Ant Design Vue 组件示例

### 1. 表单组件示例

#### 用户注册表单
```vue
<template>
  <a-card title="用户注册" style="width: 500px; margin: 0 auto;">
    <a-form
      :model="form"
      :rules="rules"
      @finish="onFinish"
      @finishFailed="onFinishFailed"
      layout="vertical"
    >
      <a-form-item label="用户名" name="username">
        <a-input v-model:value="form.username" placeholder="请输入用户名" />
      </a-form-item>
      
      <a-form-item label="邮箱" name="email">
        <a-input v-model:value="form.email" placeholder="请输入邮箱" />
      </a-form-item>
      
      <a-form-item label="密码" name="password">
        <a-input-password v-model:value="form.password" placeholder="请输入密码" />
      </a-form-item>
      
      <a-form-item label="确认密码" name="confirmPassword">
        <a-input-password v-model:value="form.confirmPassword" placeholder="请再次输入密码" />
      </a-form-item>
      
      <a-form-item label="手机号" name="phone">
        <a-input v-model:value="form.phone" placeholder="请输入手机号" />
      </a-form-item>
      
      <a-form-item label="性别" name="gender">
        <a-radio-group v-model:value="form.gender">
          <a-radio value="male">男</a-radio>
          <a-radio value="female">女</a-radio>
        </a-radio-group>
      </a-form-item>
      
      <a-form-item label="兴趣爱好" name="hobbies">
        <a-checkbox-group v-model:value="form.hobbies">
          <a-checkbox value="reading">阅读</a-checkbox>
          <a-checkbox value="music">音乐</a-checkbox>
          <a-checkbox value="sports">运动</a-checkbox>
          <a-checkbox value="travel">旅行</a-checkbox>
        </a-checkbox-group>
      </a-form-item>
      
      <a-form-item label="个人简介" name="bio">
        <a-textarea v-model:value="form.bio" :rows="4" placeholder="请输入个人简介" />
      </a-form-item>
      
      <a-form-item name="agreement">
        <a-checkbox v-model:checked="form.agreement">
          我已阅读并同意 <a href="#">用户协议</a> 和 <a href="#">隐私政策</a>
        </a-checkbox>
      </a-form-item>
      
      <a-form-item>
        <a-button type="primary" html-type="submit" :loading="loading" block>
          注册
        </a-button>
      </a-form-item>
    </a-form>
  </a-card>
</template>

<script setup lang="ts">
import { reactive, ref } from 'vue';
import { message } from 'ant-design-vue';

const loading = ref(false);

const form = reactive({
  username: '',
  email: '',
  password: '',
  confirmPassword: '',
  phone: '',
  gender: 'male',
  hobbies: [],
  bio: '',
  agreement: false
});

const rules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 3, max: 20, message: '用户名长度为3-20个字符', trigger: 'blur' }
  ],
  email: [
    { required: true, message: '请输入邮箱', trigger: 'blur' },
    { type: 'email', message: '请输入正确的邮箱格式', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, message: '密码至少6位', trigger: 'blur' }
  ],
  confirmPassword: [
    { required: true, message: '请确认密码', trigger: 'blur' },
    {
      validator: (rule: any, value: string) => {
        if (value !== form.password) {
          return Promise.reject('两次输入的密码不一致');
        }
        return Promise.resolve();
      },
      trigger: 'blur'
    }
  ],
  phone: [
    { required: true, message: '请输入手机号', trigger: 'blur' },
    { pattern: /^1[3-9]\d{9}$/, message: '请输入正确的手机号', trigger: 'blur' }
  ],
  agreement: [
    {
      validator: (rule: any, value: boolean) => {
        if (!value) {
          return Promise.reject('请同意用户协议和隐私政策');
        }
        return Promise.resolve();
      },
      trigger: 'change'
    }
  ]
};

const onFinish = async (values: any) => {
  loading.value = true;
  try {
    // 模拟 API 调用
    await new Promise(resolve => setTimeout(resolve, 2000));
    message.success('注册成功！');
    console.log('表单数据:', values);
  } catch (error) {
    message.error('注册失败，请重试');
  } finally {
    loading.value = false;
  }
};

const onFinishFailed = (errorInfo: any) => {
  console.log('表单验证失败:', errorInfo);
  message.error('请检查表单信息');
};
</script>
```

### 2. 数据展示组件示例

#### 用户管理表格
```vue
<template>
  <div>
    <a-card>
      <template #title>
        <div style="display: flex; justify-content: space-between; align-items: center;">
          <span>用户管理</span>
          <a-button type="primary" @click="addUser">
            <PlusOutlined />
            添加用户
          </a-button>
        </div>
      </template>
      
      <a-table
        :columns="columns"
        :data-source="dataSource"
        :pagination="pagination"
        :loading="loading"
        row-key="id"
        @change="handleTableChange"
      >
        <template #bodyCell="{ column, record }">
          <template v-if="column.key === 'avatar'">
            <a-avatar :src="record.avatar" :size="40">
              {{ record.name.charAt(0) }}
            </a-avatar>
          </template>
          
          <template v-if="column.key === 'status'">
            <a-tag :color="record.status === 1 ? 'green' : 'red'">
              {{ record.status === 1 ? '启用' : '禁用' }}
            </a-tag>
          </template>
          
          <template v-if="column.key === 'createTime'">
            {{ formatDate(record.createTime) }}
          </template>
          
          <template v-if="column.key === 'action'">
            <a-space>
              <a-button type="link" size="small" @click="editUser(record)">
                编辑
              </a-button>
              <a-button type="link" size="small" @click="viewUser(record)">
                查看
              </a-button>
              <a-popconfirm
                title="确定要删除这个用户吗？"
                @confirm="deleteUser(record)"
              >
                <a-button type="link" size="small" danger>
                  删除
                </a-button>
              </a-popconfirm>
            </a-space>
          </template>
        </template>
      </a-table>
    </a-card>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive, onMounted } from 'vue';
import { message, Modal } from 'ant-design-vue';
import { PlusOutlined } from '@ant-design/icons-vue';

const loading = ref(false);

const columns = [
  { title: '头像', dataIndex: 'avatar', key: 'avatar', width: 80 },
  { title: '姓名', dataIndex: 'name', key: 'name', width: 120 },
  { title: '邮箱', dataIndex: 'email', key: 'email', width: 200 },
  { title: '手机号', dataIndex: 'phone', key: 'phone', width: 150 },
  { title: '状态', dataIndex: 'status', key: 'status', width: 100 },
  { title: '创建时间', dataIndex: 'createTime', key: 'createTime', width: 180 },
  { title: '操作', key: 'action', width: 200, fixed: 'right' }
];

const dataSource = ref([
  {
    id: 1,
    name: '张三',
    email: 'zhangsan@example.com',
    phone: '13800138000',
    avatar: 'https://api.dicebear.com/7.x/miniavs/svg?seed=1',
    status: 1,
    createTime: '2024-01-01T10:00:00Z'
  },
  {
    id: 2,
    name: '李四',
    email: 'lisi@example.com',
    phone: '13800138001',
    avatar: 'https://api.dicebear.com/7.x/miniavs/svg?seed=2',
    status: 0,
    createTime: '2024-01-02T11:00:00Z'
  }
]);

const pagination = reactive({
  current: 1,
  pageSize: 10,
  total: 100,
  showSizeChanger: true,
  showQuickJumper: true,
  showTotal: (total: number, range: [number, number]) => 
    `第 ${range[0]}-${range[1]} 条/共 ${total} 条`
});

const formatDate = (dateString: string) => {
  return new Date(dateString).toLocaleString('zh-CN');
};

const handleTableChange = (pag: any) => {
  pagination.current = pag.current;
  pagination.pageSize = pag.pageSize;
  loadData();
};

const loadData = async () => {
  loading.value = true;
  try {
    // 模拟 API 调用
    await new Promise(resolve => setTimeout(resolve, 1000));
    // 更新数据源
  } finally {
    loading.value = false;
  }
};

const addUser = () => {
  console.log('添加用户');
};

const editUser = (record: any) => {
  console.log('编辑用户:', record);
};

const viewUser = (record: any) => {
  Modal.info({
    title: '用户详情',
    content: `姓名: ${record.name}\n邮箱: ${record.email}\n手机: ${record.phone}`,
    width: 500
  });
};

const deleteUser = async (record: any) => {
  try {
    // 模拟 API 调用
    await new Promise(resolve => setTimeout(resolve, 1000));
    message.success('删除成功');
    loadData();
  } catch (error) {
    message.error('删除失败');
  }
};

onMounted(() => {
  loadData();
});
</script>
```

### 3. 反馈组件示例

#### 消息提示示例
```vue
<template>
  <a-space wrap>
    <a-button @click="showSuccess">成功消息</a-button>
    <a-button @click="showError">错误消息</a-button>
    <a-button @click="showWarning">警告消息</a-button>
    <a-button @click="showInfo">信息消息</a-button>
    <a-button @click="showLoading">加载消息</a-button>
  </a-space>
</template>

<script setup lang="ts">
import { message } from 'ant-design-vue';

const showSuccess = () => {
  message.success('操作成功！');
};

const showError = () => {
  message.error('操作失败！');
};

const showWarning = () => {
  message.warning('警告信息！');
};

const showInfo = () => {
  message.info('提示信息！');
};

const showLoading = () => {
  const hide = message.loading('正在处理...', 0);
  setTimeout(() => {
    hide();
    message.success('处理完成！');
  }, 2000);
};
</script>
```

#### 通知示例
```vue
<template>
  <a-space wrap>
    <a-button @click="showSuccessNotification">成功通知</a-button>
    <a-button @click="showErrorNotification">错误通知</a-button>
    <a-button @click="showWarningNotification">警告通知</a-button>
    <a-button @click="showInfoNotification">信息通知</a-button>
    <a-button @click="showCustomNotification">自定义通知</a-button>
  </a-space>
</template>

<script setup lang="ts">
import { notification } from 'ant-design-vue';

const showSuccessNotification = () => {
  notification.success({
    message: '操作成功',
    description: '数据已成功保存到服务器',
    placement: 'topRight',
    duration: 4.5
  });
};

const showErrorNotification = () => {
  notification.error({
    message: '操作失败',
    description: '网络连接异常，请检查网络设置',
    placement: 'topRight',
    duration: 0 // 不自动关闭
  });
};

const showWarningNotification = () => {
  notification.warning({
    message: '警告',
    description: '您的账户余额不足，请及时充值',
    placement: 'topRight'
  });
};

const showInfoNotification = () => {
  notification.info({
    message: '系统通知',
    description: '系统将于今晚 22:00-24:00 进行维护',
    placement: 'topRight'
  });
};

const showCustomNotification = () => {
  notification.open({
    message: '自定义通知',
    description: '这是一个自定义样式的通知',
    placement: 'topRight',
    style: {
      backgroundColor: '#f6ffed',
      border: '1px solid #b7eb8f'
    },
    icon: '🎉'
  });
};
</script>
```

## 🔐 权限控制示例

### AccessControl 组件示例
```vue
<template>
  <div>
    <h3>权限控制示例</h3>
    
    <!-- 基于角色控制 -->
    <a-space direction="vertical" style="width: 100%;">
      <AccessControl :codes="['super']">
        <a-button type="primary">超级管理员可见</a-button>
      </AccessControl>
      
      <AccessControl :codes="['admin']">
        <a-button type="default">管理员可见</a-button>
      </AccessControl>
      
      <AccessControl :codes="['user']">
        <a-button type="dashed">普通用户可见</a-button>
      </AccessControl>
      
      <AccessControl :codes="['super', 'admin']">
        <a-button type="link">超级管理员和管理员可见</a-button>
      </AccessControl>
    </a-space>
    
    <!-- 基于权限码控制 -->
    <a-divider>权限码控制</a-divider>
    
    <a-space direction="vertical" style="width: 100%;">
      <AccessControl :codes="['AC_100100']" type="code">
        <a-button type="primary">用户管理权限</a-button>
      </AccessControl>
      
      <AccessControl :codes="['AC_100200']" type="code">
        <a-button type="default">订单管理权限</a-button>
      </AccessControl>
      
      <AccessControl :codes="['AC_100300']" type="code">
        <a-button type="dashed">财务管理权限</a-button>
      </AccessControl>
    </a-space>
  </div>
</template>

<script setup lang="ts">
import { AccessControl } from '@vben/access';
</script>
```

## 📱 响应式布局示例

### 响应式卡片布局
```vue
<template>
  <div>
    <a-row :gutter="[16, 16]">
      <a-col :xs="24" :sm="12" :md="8" :lg="6" :xl="4">
        <a-card title="用户统计" :bordered="false">
          <a-statistic
            title="总用户数"
            :value="1128"
            :precision="0"
            suffix="人"
          />
        </a-card>
      </a-col>
      
      <a-col :xs="24" :sm="12" :md="8" :lg="6" :xl="4">
        <a-card title="订单统计" :bordered="false">
          <a-statistic
            title="今日订单"
            :value="93"
            :precision="0"
            suffix="单"
          />
        </a-card>
      </a-col>
      
      <a-col :xs="24" :sm="12" :md="8" :lg="6" :xl="4">
        <a-card title="收入统计" :bordered="false">
          <a-statistic
            title="今日收入"
            :value="112893"
            :precision="2"
            prefix="¥"
          />
        </a-card>
      </a-col>
      
      <a-col :xs="24" :sm="12" :md="8" :lg="6" :xl="4">
        <a-card title="转化率" :bordered="false">
          <a-statistic
            title="转化率"
            :value="93.5"
            :precision="1"
            suffix="%"
          />
        </a-card>
      </a-col>
    </a-row>
  </div>
</template>
```

---

**示例集合版本**: v1.0.0  
**最后更新**: 2024年12月  
**维护者**: MPurse 开发团队
