# 核心组件开发指南

## 📋 文档概述

本文档为 MPurse 支付管理平台的核心组件开发指南，旨在帮助 AI 工程师和程序员快速上手项目开发需求。基于 Vue Vben Admin 和 Ant Design Vue 技术栈，提供完整的组件使用规范和开发最佳实践。

## 🎯 技术栈概览

### 核心技术
- **Vue 3.5.13** + **TypeScript 5.7.3**
- **Vite 6.0.11** + **Turbo 2.4.0**
- **Ant Design Vue 4.2.6**
- **Tailwind CSS 3.4.17**
- **Pinia 2.3.1** (状态管理)
- **VXE Table 4.10.0** (表格组件)

### 项目架构
- **Monorepo 结构** - 使用 pnpm workspace + Turbo
- **应用层**: `apps/mpurse-merchant` (商户端应用)
- **核心包**: `packages/` (包含 @core, effects, constants 等)
- **共享模块**: `shared/` (包含 adapter, components, request 等)

## 🏗️ Vue Vben Admin 核心组件

### 1. VbenModal - 模态框组件

#### 基础用法
```typescript
import { useVbenModal } from '@vben/common-ui';

const [Modal, modalApi] = useVbenModal({
  title: '标题',
  onConfirm: () => {
    console.log('确认操作');
  },
  onCancel: () => {
    console.log('取消操作');
  }
});
```

#### 高级用法 - 连接组件
```typescript
const [Modal, modalApi] = useVbenModal({
  title: '用户编辑',
  connectedComponent: UserEditComponent,
  onConfirm: () => {
    const formData = modalApi.getData();
    console.log('表单数据:', formData);
  }
});

// 在连接组件中使用
import { useVbenModalContext } from '@vben/components';

export default defineComponent({
  setup() {
    const { getData, setData } = useVbenModalContext();
    
    const handleSave = () => {
      setData({ name: '新用户名' });
    };
    
    return { handleSave };
  }
});
```

#### ModalApi 方法
```typescript
// 动态设置状态
modalApi.setState({ loading: true });

// 打开/关闭模态框
modalApi.open();
modalApi.close();

// 数据共享
modalApi.setData({ key: 'value' });
const data = modalApi.getData();

// 锁定/解锁模态框
modalApi.lock(true);  // 锁定
modalApi.unlock();    // 解锁
```

### 2. VbenDrawer - 抽屉组件

#### 基础用法
```typescript
import { useVbenDrawer } from '@vben/common-ui';

const [Drawer, drawerApi] = useVbenDrawer({
  title: '抽屉标题',
  placement: 'right',
  width: 600,
  onConfirm: () => {
    console.log('确认操作');
  }
});
```

#### 抽屉配置选项
```typescript
const [Drawer, drawerApi] = useVbenDrawer({
  // 基础配置
  title: '抽屉标题',
  placement: 'right', // 'left' | 'right' | 'top' | 'bottom'
  width: 600,
  height: 400,
  
  // 功能配置
  closable: true,
  mask: true,
  destroyOnClose: false,
  
  // 事件处理
  onOpenChange: (isOpen) => {
    console.log('抽屉状态:', isOpen);
  },
  onConfirm: () => {
    console.log('确认');
  },
  onCancel: () => {
    console.log('取消');
  }
});
```

### 3. VbenVxeGrid - 表格组件

#### 基础用法
```vue
<script setup lang="ts">
import { useVbenVxeGrid } from '#/adapter/vxe-table';

const [Grid, gridApi] = useVbenVxeGrid({
  gridOptions: {
    columns: [
      { field: 'name', title: '姓名', width: 120 },
      { field: 'age', title: '年龄', width: 80 },
      { field: 'email', title: '邮箱', width: 200 }
    ],
    data: [
      { name: '张三', age: 25, email: 'zhangsan@example.com' },
      { name: '李四', age: 30, email: 'lisi@example.com' }
    ]
  }
});
</script>

<template>
  <Grid />
</template>
```

#### 带搜索表单的表格
```vue
<script setup lang="ts">
const [Grid, gridApi] = useVbenVxeGrid({
  gridOptions: {
    columns: [
      { field: 'name', title: '姓名' },
      { field: 'status', title: '状态' }
    ]
  },
  formOptions: {
    schemas: [
      { 
        field: 'name', 
        label: '姓名', 
        component: 'Input',
        componentProps: {
          placeholder: '请输入姓名'
        }
      },
      { 
        field: 'status', 
        label: '状态', 
        component: 'Select',
        componentProps: {
          options: [
            { label: '启用', value: 1 },
            { label: '禁用', value: 0 }
          ]
        }
      }
    ]
  }
});
</script>
```

#### GridApi 方法
```typescript
// 设置加载状态
gridApi.setLoading(true);

// 更新表格配置
gridApi.setGridOptions({
  columns: newColumns
});

// 重新加载数据
gridApi.reload({ page: 1 });

// 查询数据（保持分页状态）
gridApi.query({ status: 1 });

// 切换搜索表单显示
gridApi.toggleSearchForm();
```

### 4. ApiComponent - API 组件

#### 基础用法
```vue
<template>
  <ApiComponent
    :api="fetchUserList"
    :component="Select"
    label-field="name"
    value-field="id"
    immediate
  />
</template>

<script setup>
import { ApiComponent } from '@vben/common-ui';
import { Select } from 'ant-design-vue';

const fetchUserList = async () => {
  const response = await fetch('/api/users');
  return response.json();
};
</script>
```

#### 级联选择器用法
```vue
<template>
  <ApiComponent
    :api="fetchRegionData"
    :component="Cascader"
    children-field="children"
    loading-slot="suffixIcon"
    visible-event="onDropdownVisibleChange"
  />
</template>

<script setup>
import { Cascader } from 'ant-design-vue';

const fetchRegionData = async () => {
  return [
    {
      label: '浙江',
      value: 'zhejiang',
      children: [
        {
          value: 'hangzhou',
          label: '杭州',
          children: [
            { value: 'xihu', label: '西湖' }
          ]
        }
      ]
    }
  ];
};
</script>
```

#### ApiComponent 配置选项
```typescript
interface ApiComponentProps {
  // 基础配置
  api: (arg?: any) => Promise<OptionsItem[] | Record<string, any>>;
  component: Component;
  immediate?: boolean; // 是否立即调用 API
  
  // 数据映射
  labelField?: string; // 标签字段名
  valueField?: string; // 值字段名
  childrenField?: string; // 子数据字段名
  resultField?: string; // API 响应中的结果字段
  
  // 组件配置
  optionsPropName?: string; // 组件接收选项的属性名
  modelPropName?: string; // 双向绑定的属性名
  
  // 事件处理
  visibleEvent?: string; // 触发重新获取数据的事件名
  beforeFetch?: (params: any) => any; // 请求前处理
  afterFetch?: (data: any) => any; // 请求后处理
}
```

## 🎨 Ant Design Vue 核心组件

### 1. 表单组件

#### 基础表单
```vue
<template>
  <a-form
    :model="form"
    :rules="rules"
    @finish="onFinish"
    @finishFailed="onFinishFailed"
  >
    <a-form-item label="用户名" name="username">
      <a-input v-model:value="form.username" />
    </a-form-item>
    
    <a-form-item label="密码" name="password">
      <a-input-password v-model:value="form.password" />
    </a-form-item>
    
    <a-form-item label="邮箱" name="email">
      <a-input v-model:value="form.email" />
    </a-form-item>
    
    <a-form-item>
      <a-button type="primary" html-type="submit">提交</a-button>
    </a-form-item>
  </a-form>
</template>

<script setup lang="ts">
import { reactive } from 'vue';

const form = reactive({
  username: '',
  password: '',
  email: ''
});

const rules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, message: '密码至少6位', trigger: 'blur' }
  ],
  email: [
    { required: true, message: '请输入邮箱', trigger: 'blur' },
    { type: 'email', message: '请输入正确的邮箱格式', trigger: 'blur' }
  ]
};

const onFinish = (values: any) => {
  console.log('表单提交:', values);
};

const onFinishFailed = (errorInfo: any) => {
  console.log('表单验证失败:', errorInfo);
};
</script>
```

#### 高级表单组件
```vue
<template>
  <a-form :model="form">
    <!-- 选择器 -->
    <a-form-item label="状态">
      <a-select v-model:value="form.status" placeholder="请选择状态">
        <a-select-option value="active">启用</a-select-option>
        <a-select-option value="inactive">禁用</a-select-option>
      </a-select>
    </a-form-item>
    
    <!-- 日期选择器 -->
    <a-form-item label="创建时间">
      <a-date-picker v-model:value="form.createTime" />
    </a-form-item>
    
    <!-- 时间范围选择器 -->
    <a-form-item label="时间范围">
      <a-range-picker v-model:value="form.timeRange" />
    </a-form-item>
    
    <!-- 数字输入框 -->
    <a-form-item label="数量">
      <a-input-number v-model:value="form.quantity" :min="0" :max="100" />
    </a-form-item>
    
    <!-- 开关 -->
    <a-form-item label="是否启用">
      <a-switch v-model:checked="form.enabled" />
    </a-form-item>
    
    <!-- 单选框组 -->
    <a-form-item label="类型">
      <a-radio-group v-model:value="form.type">
        <a-radio value="type1">类型1</a-radio>
        <a-radio value="type2">类型2</a-radio>
      </a-radio-group>
    </a-form-item>
    
    <!-- 复选框组 -->
    <a-form-item label="权限">
      <a-checkbox-group v-model:value="form.permissions">
        <a-checkbox value="read">读取</a-checkbox>
        <a-checkbox value="write">写入</a-checkbox>
        <a-checkbox value="delete">删除</a-checkbox>
      </a-checkbox-group>
    </a-form-item>
    
    <!-- 上传组件 -->
    <a-form-item label="头像">
      <a-upload
        v-model:file-list="form.avatar"
        list-type="picture-card"
        :max-count="1"
      >
        <div>
          <PlusOutlined />
          <div style="margin-top: 8px">上传</div>
        </div>
      </a-upload>
    </a-form-item>
  </a-form>
</template>
```

### 2. 数据展示组件

#### 表格组件
```vue
<template>
  <a-table
    :columns="columns"
    :data-source="dataSource"
    :pagination="pagination"
    :loading="loading"
    row-key="id"
    @change="handleTableChange"
  >
    <!-- 自定义列渲染 -->
    <template #bodyCell="{ column, record }">
      <template v-if="column.key === 'status'">
        <a-tag :color="record.status === 1 ? 'green' : 'red'">
          {{ record.status === 1 ? '启用' : '禁用' }}
        </a-tag>
      </template>
      
      <template v-if="column.key === 'action'">
        <a-space>
          <a-button type="link" @click="edit(record)">编辑</a-button>
          <a-button type="link" danger @click="delete(record)">删除</a-button>
        </a-space>
      </template>
    </template>
  </a-table>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue';

const columns = [
  { title: '姓名', dataIndex: 'name', key: 'name' },
  { title: '年龄', dataIndex: 'age', key: 'age' },
  { title: '邮箱', dataIndex: 'email', key: 'email' },
  { title: '状态', dataIndex: 'status', key: 'status' },
  { title: '操作', key: 'action' }
];

const dataSource = ref([
  { id: 1, name: '张三', age: 25, email: 'zhangsan@example.com', status: 1 },
  { id: 2, name: '李四', age: 30, email: 'lisi@example.com', status: 0 }
]);

const pagination = reactive({
  current: 1,
  pageSize: 10,
  total: 100,
  showSizeChanger: true,
  showQuickJumper: true
});

const loading = ref(false);

const handleTableChange = (pag: any) => {
  pagination.current = pag.current;
  pagination.pageSize = pag.pageSize;
  // 重新加载数据
  loadData();
};

const edit = (record: any) => {
  console.log('编辑:', record);
};

const delete = (record: any) => {
  console.log('删除:', record);
};

const loadData = async () => {
  loading.value = true;
  try {
    // 调用 API 加载数据
    // const response = await fetchUsers(pagination);
    // dataSource.value = response.data;
    // pagination.total = response.total;
  } finally {
    loading.value = false;
  }
};
</script>
```

#### 卡片组件
```vue
<template>
  <a-row :gutter="16">
    <a-col :span="8">
      <a-card title="用户统计" :bordered="false">
        <a-statistic
          title="总用户数"
          :value="1128"
          :precision="0"
          suffix="人"
        />
      </a-card>
    </a-col>
    
    <a-col :span="8">
      <a-card title="订单统计" :bordered="false">
        <a-statistic
          title="今日订单"
          :value="93"
          :precision="0"
          suffix="单"
        />
      </a-card>
    </a-col>
    
    <a-col :span="8">
      <a-card title="收入统计" :bordered="false">
        <a-statistic
          title="今日收入"
          :value="112893"
          :precision="2"
          prefix="¥"
        />
      </a-card>
    </a-col>
  </a-row>
</template>
```

### 3. 反馈组件

#### 消息提示
```vue
<script setup lang="ts">
import { App } from 'ant-design-vue';

const { message, modal, notification } = App.useApp();

// 消息提示
const showMessage = () => {
  message.success('操作成功！');
  message.error('操作失败！');
  message.warning('警告信息！');
  message.info('提示信息！');
};

// 模态框
const showModal = () => {
  modal.confirm({
    title: '确认删除',
    content: '确定要删除这条记录吗？',
    onOk() {
      console.log('确认删除');
    },
    onCancel() {
      console.log('取消删除');
    }
  });
};

// 通知
const showNotification = () => {
  notification.open({
    message: '通知标题',
    description: '这是一条通知的描述信息',
    placement: 'topRight',
    duration: 4.5
  });
};
</script>

<template>
  <a-space>
    <a-button @click="showMessage">消息提示</a-button>
    <a-button @click="showModal">确认框</a-button>
    <a-button @click="showNotification">通知</a-button>
  </a-space>
</template>
```

## 🔐 权限控制组件

### AccessControl 组件
```vue
<template>
  <!-- 基于角色控制 -->
  <AccessControl :codes="['super', 'admin']">
    <a-button type="primary">管理员可见</a-button>
  </AccessControl>
  
  <!-- 基于权限码控制 -->
  <AccessControl :codes="['AC_100100']" type="code">
    <a-button>权限码控制</a-button>
  </AccessControl>
  
  <!-- 多权限控制 -->
  <AccessControl :codes="['super', 'admin', 'user']">
    <a-button>多角色可见</a-button>
  </AccessControl>
</template>

<script setup lang="ts">
import { AccessControl } from '@vben/access';
</script>
```

## 📝 开发规范

### 1. 组件命名规范
- **Vue 组件**: 使用 PascalCase (如 `UserProfile.vue`)
- **工具函数**: 使用 camelCase (如 `formatUtils.ts`)
- **常量文件**: 使用 UPPER_SNAKE_CASE (如 `API_CONSTANTS.ts`)

### 2. TypeScript 规范
```typescript
// 接口定义
interface UserInfo {
  id: number;
  name: string;
  email: string;
  status: 0 | 1;
}

// 组件 Props 定义
interface UserFormProps {
  userInfo?: UserInfo;
  loading?: boolean;
  onSubmit?: (data: UserInfo) => void;
}

// 组件定义
export default defineComponent<UserFormProps>({
  name: 'UserForm',
  props: {
    userInfo: {
      type: Object as PropType<UserInfo>,
      default: () => ({})
    },
    loading: {
      type: Boolean,
      default: false
    },
    onSubmit: {
      type: Function as PropType<(data: UserInfo) => void>,
      default: () => {}
    }
  }
});
```

### 3. JSDoc 注释规范
```typescript
/**
 * 用户信息格式化工具
 * @param user - 用户信息对象
 * @param format - 格式化类型
 * @returns 格式化后的用户信息字符串
 * @example
 * ```typescript
 * const formatted = formatUserInfo(user, 'full');
 * console.log(formatted); // "张三 (zhangsan@example.com)"
 * ```
 */
export function formatUserInfo(user: UserInfo, format: 'full' | 'simple'): string {
  // 实现逻辑
}
```

### 4. 样式规范
```vue
<template>
  <div class="user-profile">
    <div class="user-profile__header">
      <h2 class="user-profile__title">用户信息</h2>
    </div>
    <div class="user-profile__content">
      <!-- 内容 -->
    </div>
  </div>
</template>

<style scoped>
.user-profile {
  @apply bg-white rounded-lg shadow-sm p-6;
}

.user-profile__header {
  @apply border-b border-gray-200 pb-4 mb-6;
}

.user-profile__title {
  @apply text-xl font-semibold text-gray-900;
}

.user-profile__content {
  @apply space-y-4;
}
</style>
```

## 🚀 最佳实践

### 1. 组件复用
- 优先使用 `@shared/*` 共享模块
- 新增公共功能放在 `packages/` 或 `shared/`
- 避免重复代码，提取公共逻辑

### 2. 性能优化
- 使用 `v-memo` 缓存渲染结果
- 合理使用 `v-once` 渲染一次性内容
- 列表使用正确的 `key` 属性
- 路由懒加载

### 3. 错误处理
```typescript
// API 请求错误处理
const handleApiError = (error: any) => {
  if (error.response?.status === 401) {
    // 未授权，跳转登录页
    router.push('/login');
  } else if (error.response?.status === 403) {
    // 无权限，提示用户
    message.error('无权限访问');
  } else if (error.response?.status >= 500) {
    // 服务器错误
    message.error('服务器错误，请稍后重试');
  } else {
    // 其他错误
    message.error(error.message || '请求失败');
  }
};
```

### 4. 状态管理
```typescript
// Pinia Store 示例
export const useUserStore = defineStore('user', () => {
  const userInfo = ref<UserInfo | null>(null);
  const loading = ref(false);
  
  const login = async (credentials: LoginCredentials) => {
    loading.value = true;
    try {
      const response = await authApi.login(credentials);
      userInfo.value = response.data;
      return response.data;
    } catch (error) {
      handleApiError(error);
      throw error;
    } finally {
      loading.value = false;
    }
  };
  
  const logout = () => {
    userInfo.value = null;
    router.push('/login');
  };
  
  return {
    userInfo: readonly(userInfo),
    loading: readonly(loading),
    login,
    logout
  };
});
```

## 📚 参考资源

- [Vue Vben Admin 官方文档](https://github.com/vbenjs/vue-vben-admin)
- [Ant Design Vue 官方文档](https://antdv.com/docs/vue/introduce)
- [Vue 3 官方文档](https://v3.cn.vuejs.org/)
- [Pinia 官方文档](https://pinia.vuejs.org/)
- [Tailwind CSS 官方文档](https://tailwindcss.com/)

## 🔧 开发工具

### 常用命令
```bash
# 开发环境
pnpm dev:mpurse-merchant

# 构建
pnpm build:qa
pnpm build:uat
pnpm build:prod

# 代码质量
pnpm lint
pnpm format
pnpm typecheck

# 测试
pnpm test:unit
pnpm test:e2e
```

### 环境变量
```bash
# .env.development
VITE_GLOB_API_URL='开发环境API地址'
VITE_NEED_ENCRYPT=false

# .env.qa
VITE_GLOB_API_URL='https://qaapi.mpurse.co.in/'
VITE_NEED_ENCRYPT=false

# .env.prod
VITE_GLOB_API_URL='生产环境API地址'
VITE_NEED_ENCRYPT=true
```

---

**文档版本**: v1.0.0  
**最后更新**: 2024年12月  
**维护者**: MPurse 开发团队
